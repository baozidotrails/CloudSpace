<div class="container">
  <%= form_for @user, html: { class: 'main_sign_up_form error_form' } do |f| %>
    <!-- if it goes wrong -->
    <% @error_form = f.object %>
    <div>
      <p><%= f.text_field :username, placeholder: '名字' %></p>
      <p><%= f.email_field :email, placeholder: '電子郵件' %></p>
      <p><%= f.password_field :password, placeholder: '密碼' %></p>
      <p><%= f.password_field :password_confirmation, placeholder: '確認密碼' %></p>
      <p>
        <%= f.submit '確認', class: 'btn' %>
      </p>
    </div>
  <% end %>
  <div><%= render 'shared/error_msg', object: @error_form %></div>
</div>